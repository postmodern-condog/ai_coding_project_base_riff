# Ignore session logs (may contain sensitive data)
sessions.jsonl

# Ignore analysis marker
.last-analysis-count

# Keep the directory structure
!.gitkeep
!.gitignore

# Keep analysis reports (they're useful to share)
!ANALYSIS_REPORT.md
